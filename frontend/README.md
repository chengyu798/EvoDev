# 前端应用

本目录是 EvoDev 的 React + TypeScript + Vite 前端工程。当前页面展示系统定位、工作流概览和
API/Docker 健康状态；任务创建与执行界面仍待实现。

```bash
npm install
npm run dev
npm run lint
npm run test
npm run build
```

接口请求放在 `src/api/`，可复用视图组件放在 `src/components/`，测试公共配置放在
`src/test/`。
