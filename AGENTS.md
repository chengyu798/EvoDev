# 仓库贡献指南

## 项目结构与模块组织

EvoDev 是一个处于早期阶段的 Python 3.13 项目，目标是构建基于 LangGraph 的多智能体软件修复系统。Python 包代码位于 `src/evodev/`。新增模块应归入已有的领域目录，例如 `agents/`、`application/`、`domain/`、`runtime/`、`tools/` 和 `workflows/`。HTTP 接口放在 `src/evodev/api/`，持久化逻辑放在 `persistence/`，评测与运行轨迹代码分别放在 `evaluation/` 和 `observability/`。

Python 测试统一放在 `tests/`，目录结构应尽量与源码对应。前端代码放在 `frontend/src/`，接口客户端和可复用组件分别放在现有的 `api/` 与 `components/` 目录。架构决策和项目文档位于 `docs/`；新增文档应按 `03-接口设计.md` 的格式连续编号。运行输出应写入 `outputs/` 或 `data/`，不要混入源码目录。

## 构建、测试与本地开发

本项目使用 `uv` 管理环境，并采用 `src` 包布局。

- `uv sync`：根据 `pyproject.toml` 创建或更新本地环境。
- `uv run evodev`：运行当前的命令行入口。
- `uv run --with pytest pytest`：在尚未声明测试依赖时运行测试套件。
- `uv build`：构建源码包和 wheel 包。

长期使用的运行或开发依赖应写入 `pyproject.toml`，不要依赖全局安装的软件包。

## 编码风格与命名约定

Python 代码使用四个空格缩进；公共接口应提供类型标注；仅在行为不直观时添加简短文档字符串。遵循 PEP 8：模块、函数和变量使用 `snake_case`，类使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`。Git 操作、测试执行和文件访问等确定性逻辑应与 Agent 推理分离。目前仓库尚未配置格式化或静态检查工具，因此请保持导入有序、改动聚焦。

## 测试规范

测试框架使用 `pytest`。测试文件命名为 `test_<模块>.py`，测试函数命名为 `test_<行为>()`。工作流与沙箱代码应覆盖成功、失败、超时和重试路径。测试不得修改真实仓库；应使用临时工作区，并模拟外部大模型调用。目前没有强制覆盖率门槛，但新增行为必须附带针对性测试。

## 提交与合并请求规范

当前工作副本无法读取 Git 历史，因此提交信息采用简短、祈使式标题，可按模块添加前缀，例如 `workflow: 添加重试路由`。每个提交应只包含一组相关改动。合并请求需说明问题与解决方案，列出验证命令并关联相关 Issue；前端改动需附截图。涉及数据结构、配置或沙箱安全的变化必须明确标注。
